<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.maho_ya.tell_me_your_dpi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.maho_ya.tell_me_your_dpi.MainApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:dataExtractionRules="@xml/data_extraction_rules"
        tools:ignore="DataExtractionRules"
        tools:targetApi="s">

        <activity android:name="com.maho_ya.tell_me_your_dpi.ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- It needs to be overridden theme. The title doesn't appear because the theme style is set to NoActionBar.-->
        <activity
            android:theme="@style/OssLicenseTheme"
            android:name="com.google.android.gms.oss.licenses.OssLicensesActivity"
            android:exported="false" />
        <activity
            android:theme="@style/OssLicenseTheme"
            android:name="com.google.android.gms.oss.licenses.OssLicensesMenuActivity"
            android:exported="false" />

    </application>

</manifest>